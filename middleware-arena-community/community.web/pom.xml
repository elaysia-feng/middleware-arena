<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mware</groupId>
        <artifactId>community</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>community.web</artifactId>
    <name>community.web</name>
    <description>社区服务 Controller + 启动类</description>

    <dependencies>
        <dependency>
            <groupId>com.mware</groupId>
            <artifactId>community.biz</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mware</groupId>
            <artifactId>base.web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- ShardingSphere-JDBC 5.5.3：分库分表（点赞高并发实验）
             JDBC Driver 模式（低耦合）：Spring Boot 只把 jdbc:shardingsphere:... 当普通数据源，
             分片由 ShardingSphereDriver 按 application-sharding.yml 规则拦截改写。
             5.5.x 已移除 spring.shardingsphere.* starter 自动配置，故无 starter 依赖。
             shardingsphere-jdbc 是 Driver 壳；shardingsphere-sharding-core 是分片规则引擎
             （在 jdbc 的 pom 里是 test scope，运行时分片必须显式声明）。
             已离线下载入 MVN_repo（mvn dependency:get 直连 Central），离线编译无碍。 -->
        <dependency>
            <groupId>org.apache.shardingsphere</groupId>
            <artifactId>shardingsphere-jdbc</artifactId>
            <version>5.5.3</version>
        </dependency>
        <dependency>
            <groupId>org.apache.shardingsphere</groupId>
            <artifactId>shardingsphere-sharding-core</artifactId>
            <version>5.5.3</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
