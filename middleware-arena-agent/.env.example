# 复制说明用途；仓库中的 .env 已提供非敏感开发默认值。
# 真实密钥建议写 middleware-arena-agent/.env.local，不提交 Git。

# ==================== Agent ====================
AGENT_HOST=0.0.0.0
AGENT_PORT=9500
AGENT_HTTP_PREFIX=/agent
AGENT_MAX_ANALYSIS_ITERATIONS=3
AGENT_PATCH_CONFIDENCE_THRESHOLD=0.75
AGENT_MQ_ENABLED=false
AGENT_MQ_PREFETCH=1
AGENT_MQ_MAX_ATTEMPTS=3
AGENT_MQ_RETRY_INTERVAL_SECONDS=1.0

# ==================== RabbitMQ ====================
RABBIT_HOST=192.168.0.192
RABBIT_PORT=5672
RABBIT_USER=guest
RABBIT_PASSWORD=guest
RABBIT_VHOST=/

# ==================== Java internal APIs ====================
EXPERIMENT_SERVICE_URL=http://127.0.0.1:9003
MA_INTERNAL_TOKEN=middleware-arena-internal-token
INTERNAL_TOKEN_HEADER=X-Internal-Token
INTERNAL_HTTP_TIMEOUT_SECONDS=10

# ==================== LLM ====================
# Agent 使用 MiniMax 的 Anthropic 兼容接口；本机未配置 Key 时读取 .claude/settings.json。
ANTHROPIC_BASE_URL=https://api.minimaxi.com/anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=MiniMax-M3[1m]

# ==================== Langfuse ====================
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=http://127.0.0.1:3000
LANGFUSE_TRACING_ENVIRONMENT=development
